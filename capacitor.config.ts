import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.purple4reina.ptp',
  appName: 'Practice to Perfection',
  webDir: 'docs',
};

export default config;
